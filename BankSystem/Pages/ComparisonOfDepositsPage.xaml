<Page x:Class="BankSystem.Pages.ComparisonOfDepositsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BankSystem.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ComparisonOfDepositsPage">

    <Grid>
        <ListView HorizontalAlignment="Left" Height="280" Margin="0,10,0,0" VerticalAlignment="Top" Width="604">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ListBoxItem>
                        <ListBoxItem.Template>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border BorderThickness="0,0,0,1" BorderBrush="AliceBlue">
                                    
                                </Border>
                            </ControlTemplate>
                        </ListBoxItem.Template>
                    </ListBoxItem>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
